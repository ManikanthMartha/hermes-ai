# Eval suite (built in Phase 5). Install with:
#   cd eval && python -m venv .venv && source .venv/Scripts/activate && pip install -r requirements.txt
ragas>=0.2
anthropic>=0.40
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0
